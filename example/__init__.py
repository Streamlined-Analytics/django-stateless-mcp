"""A tiny runnable Django project demonstrating django-stateless-mcp.

Launch it with `just demo` (WSGI) or `just demo-asgi` (multi-worker ASGI);
the test suite reuses its settings, URLs and MCP servers as fixtures.
"""
