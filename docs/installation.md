# Installation

## Stable release

To install django-stateless-mcp, run this command in your terminal:

```sh
uv add django-stateless-mcp
```

Or if you prefer to use `pip`:

```sh
pip install django-stateless-mcp
```

## From source

Working on the package itself? See
[CONTRIBUTING.md](https://github.com/Streamlined-Analytics/django-stateless-mcp/blob/main/CONTRIBUTING.md)
for the development setup.
