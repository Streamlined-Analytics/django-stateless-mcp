# Known-failure baseline; the runner fails on stale entries, so it burns down. See ADR-0008.
client: []

server:
  # dns-rebinding-protection passes since the consumer-shaped middleware stack:
  # CommonMiddleware calls get_host(), so ALLOWED_HOSTS rejects evil hosts (ADR-0007, ADR-0032).

  # Fixture gaps: tests/mcp_server.py lacks the scenario's test_* tools; close per phase.
  - completion-complete
  - tools-call-image
  - tools-call-audio
  - tools-call-embedded-resource
  - tools-call-mixed-content
  - tools-call-with-progress
  - json-schema-2020-12
  - resources-read-text
  - resources-read-binary
  - resources-templates-read
  - prompts-get-simple
  - prompts-get-with-args
  - prompts-get-embedded-resource
  - prompts-get-with-image
  - http-custom-header-server-validation
